
name: test echo

# events
on:
  push: # run when push on any branch
  pull_request:
    types: [ opened, reopened]

# jobs
jobs:
  echo_job:
    name: first job
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Hello CI! 11"
      - name: second step
        run: echo "Hello CI! 12"
  echo_job_2:
    name: second job
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Hello CI! 21"
